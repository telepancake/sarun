# Phase 3 — Test

Read `gitdepot/pipeline/00-context.md`, `gitdepot/DESIGN.md`, and
`gitdepot/IMPL-NOTES.md`.

Build and run the lib and integration tests. Report pass/fail counts and quote
any failing output; do not hide failures. Where a guarantee the design states is
not covered by a test, add one. Only add/adjust tests — do not change production
code to make a test pass. Return the build status, the counts, and any failures.
