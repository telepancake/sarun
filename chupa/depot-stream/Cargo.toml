[package]
name = "depot-stream"
version = "0.0.0"
edition.workspace = true
license.workspace = true
publish = false

[dependencies]
depot = { path = "../depot" }
zstd = { workspace = true, features = ["experimental"] }

[dev-dependencies]
tempfile = "3"
